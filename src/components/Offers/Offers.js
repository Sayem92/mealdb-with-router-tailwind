import React from 'react';
import Header from '../Header/Header';

const Offers = () => {
    return (
        <div>
            <Header></Header>
            <h1>This is offers page here</h1>
        </div>
    );
};

export default Offers;