import React from 'react';
import Header from '../Header/Header';

const Rewards = () => {
    return (
        <div>
            <Header></Header>
            <h1>this is rewards component</h1>
        </div>
    );
};

export default Rewards;